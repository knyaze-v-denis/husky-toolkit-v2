From 40232f678d2ab7082ca178e60649ac257ae1b306 Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Thu, 25 Jun 2026 14:37:22 +0000
Subject: [PATCH] Unify build cards, fix risks display, sync active colors,
 reduce top spacing

---
 app/builder/page.module.css                 |  2 +-
 components/builder/PageHeader.module.css    |  2 +-
 components/builder/StepArtifacts.module.css | 27 ++++++++------------
 components/builder/StepArtifacts.tsx        |  7 +++---
 components/builder/StepEB.module.css        |  8 +++---
 components/builder/StepEB.tsx               | 15 ++++++++---
 components/builder/StepResult.module.css    |  9 ++++++-
 components/builder/StepResult.tsx           | 28 +++++++++++----------
 8 files changed, 54 insertions(+), 44 deletions(-)

diff --git a/app/builder/page.module.css b/app/builder/page.module.css
index 23f8557..bda6747 100644
--- a/app/builder/page.module.css
+++ b/app/builder/page.module.css
@@ -3,7 +3,7 @@
   top: 0;
   z-index: 10;
   background: #f7f7f5;
-  padding-top: 1.5rem;
+  padding-top: 0.75rem;
   padding-bottom: 4px;
 }
 
diff --git a/components/builder/PageHeader.module.css b/components/builder/PageHeader.module.css
index deed04a..12ed11e 100644
--- a/components/builder/PageHeader.module.css
+++ b/components/builder/PageHeader.module.css
@@ -9,7 +9,7 @@
 .sticky {
   position: sticky;
   top: 0;
-  padding-top: 1.5rem;
+  padding-top: 0.75rem;
   z-index: 10;
 }
 
diff --git a/components/builder/StepArtifacts.module.css b/components/builder/StepArtifacts.module.css
index 2df6c2f..ebf6df7 100644
--- a/components/builder/StepArtifacts.module.css
+++ b/components/builder/StepArtifacts.module.css
@@ -9,30 +9,25 @@
   padding: 18px;
 }
 
-.buildMeta {
-  display: flex;
-  align-items: center;
-  justify-content: space-between;
-  margin-bottom: 8px;
-}
-
-.buildType {
+.buildLabel {
   font-size: 11px;
   font-weight: 600;
   text-transform: uppercase;
   letter-spacing: 0.5px;
-  opacity: 0.7;
+  opacity: 0.65;
+  margin-bottom: 4px;
 }
 
-.buildBadge {
-  font-size: 12px;
-  font-weight: 500;
-  opacity: 0.8;
+.buildName {
+  font-size: 22px;
+  font-weight: 700;
+  margin-bottom: 2px;
 }
 
-.buildName {
-  font-size: 18px;
-  font-weight: 600;
+.buildType {
+  font-size: 12px;
+  font-weight: 500;
+  opacity: 0.7;
   margin-bottom: 6px;
 }
 
diff --git a/components/builder/StepArtifacts.tsx b/components/builder/StepArtifacts.tsx
index 0c46562..5dc1d7b 100644
--- a/components/builder/StepArtifacts.tsx
+++ b/components/builder/StepArtifacts.tsx
@@ -31,10 +31,9 @@ export function StepArtifacts({ build, onNext, onBack }: StepArtifactsProps) {
     <div className={styles.wrap}>
       {/* Карточка билда */}
       <div className={styles.buildCard} style={{ background: b.color }}>
-        <div className={styles.buildMeta}>
-          <span className={styles.buildType} style={{ color: b.tc }}>{b.type}</span>
-        </div>
-        <div className={styles.buildName} style={{ color: b.tc }}>{b.name} билд</div>
+        <div className={styles.buildLabel} style={{ color: b.tc }}>Билд проектирования</div>
+        <div className={styles.buildName} style={{ color: b.tc }}>{b.name}</div>
+        <div className={styles.buildType} style={{ color: b.tc }}>{b.type}</div>
         <div className={styles.buildDesc} style={{ color: b.tc }}>{b.desc}</div>
       </div>
 
diff --git a/components/builder/StepEB.module.css b/components/builder/StepEB.module.css
index c392e52..406b61a 100644
--- a/components/builder/StepEB.module.css
+++ b/components/builder/StepEB.module.css
@@ -65,7 +65,7 @@
 }
 
 .optRow.optActive {
-  background: #f5f5f5;
+  background: var(--active-bg, #f5f5f5);
 }
 
 .radio {
@@ -79,7 +79,7 @@
 }
 
 .radio.radioActive {
-  border: 5px solid #3C3489;
+  border: 5px solid var(--active-color, #3C3489);
 }
 
 .checkbox {
@@ -96,8 +96,8 @@
 }
 
 .checkbox.checkboxActive {
-  background: #3C3489;
-  border-color: #3C3489;
+  background: var(--active-color, #3C3489);
+  border-color: var(--active-color, #3C3489);
 }
 
 .optText {
diff --git a/components/builder/StepEB.tsx b/components/builder/StepEB.tsx
index 777a14b..08ad2f2 100644
--- a/components/builder/StepEB.tsx
+++ b/components/builder/StepEB.tsx
@@ -15,6 +15,13 @@ interface StepEBProps {
   onBack: () => void;
 }
 
+function hexToRgba(hex: string, alpha: number): string {
+  const r = parseInt(hex.slice(1, 3), 16);
+  const g = parseInt(hex.slice(3, 5), 16);
+  const b = parseInt(hex.slice(5, 7), 16);
+  return `rgba(${r},${g},${b},${alpha})`;
+}
+
 export function StepEB({
   build, complexity, novelty, risks,
   onFactor, onRisk, onNext, onBack,
@@ -26,7 +33,7 @@ export function StepEB({
     <div className={styles.wrap}>
 
       {/* Объём работ */}
-      <div className={styles.block}>
+      <div className={styles.block} style={{ '--active-bg': hexToRgba(b.color, 0.35), '--active-color': b.tc } as React.CSSProperties}>
         <div className={styles.blockHead} style={{ background: b.color, color: b.tc }}>
           <span>Объём работ</span>
           <span className={styles.blockHint}>Определено автоматически</span>
@@ -42,7 +49,7 @@ export function StepEB({
       </div>
 
       {/* Сложность */}
-      <div className={styles.block}>
+      <div className={styles.block} style={{ '--active-bg': hexToRgba(EB_COMPLEXITY.color, 0.45), '--active-color': EB_COMPLEXITY.tc } as React.CSSProperties}>
         <div
           className={styles.blockHead}
           style={{ background: EB_COMPLEXITY.color, color: EB_COMPLEXITY.tc }}
@@ -70,7 +77,7 @@ export function StepEB({
       </div>
 
       {/* Новизна */}
-      <div className={styles.block}>
+      <div className={styles.block} style={{ '--active-bg': hexToRgba(EB_NOVELTY.color, 0.45), '--active-color': EB_NOVELTY.tc } as React.CSSProperties}>
         <div
           className={styles.blockHead}
           style={{ background: EB_NOVELTY.color, color: EB_NOVELTY.tc }}
@@ -98,7 +105,7 @@ export function StepEB({
       </div>
 
       {/* Риски */}
-      <div className={styles.block}>
+      <div className={styles.block} style={{ '--active-bg': 'rgba(60,52,137,0.08)', '--active-color': '#3C3489' } as React.CSSProperties}>
         <div className={styles.blockHead} style={{ background: '#ECEAF8', color: '#3C3489' }}>
           <span>Риски</span>
         </div>
diff --git a/components/builder/StepResult.module.css b/components/builder/StepResult.module.css
index b0fe183..72317b9 100644
--- a/components/builder/StepResult.module.css
+++ b/components/builder/StepResult.module.css
@@ -21,7 +21,14 @@
 .buildName {
   font-size: 22px;
   font-weight: 700;
-  margin-bottom: 4px;
+  margin-bottom: 2px;
+}
+
+.buildType {
+  font-size: 12px;
+  font-weight: 500;
+  opacity: 0.7;
+  margin-bottom: 6px;
 }
 
 .buildRange {
diff --git a/components/builder/StepResult.tsx b/components/builder/StepResult.tsx
index 26aae29..003a842 100644
--- a/components/builder/StepResult.tsx
+++ b/components/builder/StepResult.tsx
@@ -1,6 +1,6 @@
 'use client';
 
-import { ArrowLeft, Download } from 'lucide-react';
+import { ArrowLeft, Download, AlertTriangle } from 'lucide-react';
 import { BUILDS, EB_COMPLEXITY, EB_NOVELTY, RISK_GROUPS, fmtScore, type BuildType, type ArtStatus } from '@/lib/data/builder';
 import styles from './StepResult.module.css';
 
@@ -50,10 +50,11 @@ export function StepResult({
   return (
     <div className={styles.wrap}>
 
-      {/* Билд — без баллов, с описанием */}
+      {/* Билд */}
       <div className={styles.buildCard} style={{ background: b.color }}>
         <div className={styles.buildLabel} style={{ color: b.tc }}>Билд проектирования</div>
         <div className={styles.buildName} style={{ color: b.tc }}>{b.name}</div>
+        <div className={styles.buildType} style={{ color: b.tc }}>{b.type}</div>
         <div className={styles.buildRange} style={{ color: b.tc }}>{b.desc}</div>
       </div>
 
@@ -88,20 +89,21 @@ export function StepResult({
           </span>
         </div>
 
-        {/* Все выбранные риски */}
-        {RISK_GROUPS.map(g => {
+        {/* Выбранные риски (только не-none) */}
+        {RISK_GROUPS.flatMap(g => {
           const sel = risks[g.label] ?? [];
-          if (sel.length === 0) return null;
-          return sel.map(v => {
-            const item = g.items.find(i => i.v === v);
-            if (!item) return null;
-            return (
-              <div key={v} className={styles.row}>
-                <span>Риск ({g.label}): {item.l}</span>
+          return sel
+            .map(v => g.items.find(i => i.v === v))
+            .filter((item): item is NonNullable<typeof item> => !!item && !item.none)
+            .map(item => (
+              <div key={`${g.label}-${item.v}`} className={styles.row}>
+                <span style={{ display: 'flex', alignItems: 'center', gap: 6 }}>
+                  <AlertTriangle size={13} style={{ flexShrink: 0, opacity: 0.7 }} />
+                  Риск ({g.label}): {item.l}
+                </span>
                 <span className={styles.pts}>{item.p > 0 ? `+${item.p}` : '—'}</span>
               </div>
-            );
-          });
+            ));
         })}
 
         <div className={`${styles.row} ${styles.total}`}>
-- 
2.43.0

