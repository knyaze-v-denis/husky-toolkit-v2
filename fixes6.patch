From 6ef65b19d18673824408f4f7c5a8e3ccfc7b81d2 Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Thu, 25 Jun 2026 15:04:07 +0000
Subject: [PATCH] Fix active colors, risk dividers, ebCard typography,
 checklist color sync

---
 components/builder/StepEB.module.css          |  9 +++------
 components/builder/StepEB.tsx                 |  9 ++++-----
 components/builder/StepResult.module.css      | 11 +++++------
 components/checklist/ChecklistView.module.css |  6 +++---
 components/checklist/ChecklistView.tsx        | 13 ++++++++++++-
 5 files changed, 27 insertions(+), 21 deletions(-)

diff --git a/components/builder/StepEB.module.css b/components/builder/StepEB.module.css
index 07a0bfe..80b82bc 100644
--- a/components/builder/StepEB.module.css
+++ b/components/builder/StepEB.module.css
@@ -140,10 +140,7 @@
   color: #888;
   text-transform: uppercase;
   letter-spacing: 0.4px;
-  background: #fafafa;
-}
-
-.groupDivider {
-  height: 0.5px;
-  background: #e8e8e8;
+  background: #f2f2f2;
+  border-top: 0.5px solid #e0e0e0;
+  border-bottom: 0.5px solid #e0e0e0;
 }
diff --git a/components/builder/StepEB.tsx b/components/builder/StepEB.tsx
index e567abd..b4fabd8 100644
--- a/components/builder/StepEB.tsx
+++ b/components/builder/StepEB.tsx
@@ -34,7 +34,7 @@ export function StepEB({
     <div className={styles.wrap}>
 
       {/* Объём работ */}
-      <div className={styles.block} style={{ '--active-bg': hexToRgba(b.color, 0.35), '--active-color': b.tc } as React.CSSProperties}>
+      <div className={styles.block} style={{ '--active-bg': hexToRgba(b.color, 0.2), '--active-color': b.tc } as React.CSSProperties}>
         <div className={styles.blockHead} style={{ background: b.color, color: b.tc }}>
           <span>Объём работ</span>
           <span className={styles.blockHint}>Определено автоматически</span>
@@ -50,7 +50,7 @@ export function StepEB({
       </div>
 
       {/* Сложность */}
-      <div className={styles.block} style={{ '--active-bg': hexToRgba(EB_COMPLEXITY.color, 0.45), '--active-color': EB_COMPLEXITY.tc } as React.CSSProperties}>
+      <div className={styles.block} style={{ '--active-bg': hexToRgba(EB_COMPLEXITY.color, 0.3), '--active-color': EB_COMPLEXITY.tc } as React.CSSProperties}>
         <div
           className={styles.blockHead}
           style={{ background: EB_COMPLEXITY.color, color: EB_COMPLEXITY.tc }}
@@ -78,7 +78,7 @@ export function StepEB({
       </div>
 
       {/* Новизна */}
-      <div className={styles.block} style={{ '--active-bg': hexToRgba(EB_NOVELTY.color, 0.45), '--active-color': EB_NOVELTY.tc } as React.CSSProperties}>
+      <div className={styles.block} style={{ '--active-bg': hexToRgba(EB_NOVELTY.color, 0.3), '--active-color': EB_NOVELTY.tc } as React.CSSProperties}>
         <div
           className={styles.blockHead}
           style={{ background: EB_NOVELTY.color, color: EB_NOVELTY.tc }}
@@ -106,7 +106,7 @@ export function StepEB({
       </div>
 
       {/* Риски */}
-      <div className={styles.block} style={{ '--active-bg': hexToRgba('#FAEEDA', 0.6), '--active-color': '#633806' } as React.CSSProperties}>
+      <div className={styles.block} style={{ '--active-bg': hexToRgba('#FAEEDA', 0.35), '--active-color': '#633806' } as React.CSSProperties}>
         <div className={styles.blockHead} style={{ background: '#FAEEDA', color: '#633806' }}>
           <span>Риски</span>
         </div>
@@ -114,7 +114,6 @@ export function StepEB({
           const sel = risks[group.label] ?? [];
           return (
             <div key={group.label}>
-              {gi > 0 && <div className={styles.groupDivider} />}
               <div className={styles.groupLabel}>{group.label}</div>
               {group.items.map(item => {
                 const active = sel.includes(item.v);
diff --git a/components/builder/StepResult.module.css b/components/builder/StepResult.module.css
index 4ca7131..d1c9510 100644
--- a/components/builder/StepResult.module.css
+++ b/components/builder/StepResult.module.css
@@ -53,15 +53,14 @@
 }
 
 .ebSize {
-  font-size: 36px;
-  font-weight: 800;
-  letter-spacing: -1px;
-  margin-bottom: 4px;
+  font-size: 22px;
+  font-weight: 700;
+  margin-bottom: 2px;
 }
 
 .ebTime {
-  font-size: 16px;
-  font-weight: 600;
+  font-size: 22px;
+  font-weight: 700;
 }
 
 .ebRange {
diff --git a/components/checklist/ChecklistView.module.css b/components/checklist/ChecklistView.module.css
index 46ac551..8e7f727 100644
--- a/components/checklist/ChecklistView.module.css
+++ b/components/checklist/ChecklistView.module.css
@@ -38,7 +38,7 @@
 }
 
 .item.checked {
-  background: #f5f5f5;
+  background: var(--active-bg, #f5f5f5);
 }
 
 .check {
@@ -55,8 +55,8 @@
 }
 
 .check.checkOn {
-  background: #3C3489;
-  border-color: #3C3489;
+  background: var(--active-color, #3C3489);
+  border-color: var(--active-color, #3C3489);
 }
 
 .itemBody {
diff --git a/components/checklist/ChecklistView.tsx b/components/checklist/ChecklistView.tsx
index a54a272..1063fb4 100644
--- a/components/checklist/ChecklistView.tsx
+++ b/components/checklist/ChecklistView.tsx
@@ -5,6 +5,13 @@ import { useChecklist } from '@/lib/hooks/useChecklist';
 import { PageHeader } from '@/components/builder/PageHeader';
 import styles from './ChecklistView.module.css';
 
+function hexToRgba(hex: string, alpha: number): string {
+  const r = parseInt(hex.slice(1, 3), 16);
+  const g = parseInt(hex.slice(3, 5), 16);
+  const b = parseInt(hex.slice(5, 7), 16);
+  return `rgba(${r},${g},${b},${alpha})`;
+}
+
 interface ChecklistViewProps {
   mode: ChecklistMode;
 }
@@ -46,7 +53,11 @@ export function ChecklistView({ mode }: ChecklistViewProps) {
 
       <div>
         {cl.blocks.map(block => (
-          <div key={block.title} className={styles.block}>
+          <div
+            key={block.title}
+            className={styles.block}
+            style={{ '--active-bg': hexToRgba(block.color, 0.3), '--active-color': block.tc } as React.CSSProperties}
+          >
             <div
               className={styles.blockHead}
               style={{ background: block.color, color: block.tc }}
-- 
2.43.0

